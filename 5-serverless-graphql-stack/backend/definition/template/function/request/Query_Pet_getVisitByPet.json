#**
  Select statement for a relational database data source
*#
{
    "statements": [
        "select visits.id, visits.visit_date date, visits.description, visits.pet_id from visits,pets where visits.pet_id=pets.id and pets.id = $ctx.args.id"
    ]
}