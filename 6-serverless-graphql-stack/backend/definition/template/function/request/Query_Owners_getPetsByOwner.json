#**
  Select statement for a relational database data source
*#
{
    "statements": [
        "select pets.id,pets.name,pets.birth_date birthDate,pets.owner_id,pets.type_id,types.name type_name from pets,types where pets.type_id=types.id",
        "select id,visit_date date,pet_id,description from visits"
    ]
}