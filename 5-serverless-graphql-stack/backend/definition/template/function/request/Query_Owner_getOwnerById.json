#**
  Select statement for a relational database data source
*#
{
    "statements": [
        "select id, first_name firstName, last_name lastName, address, city, telephone from owners where id = $ctx.args.id"
    ]
}